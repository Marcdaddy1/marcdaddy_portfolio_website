# Dependencies
node_modules/

# Logs
logs
*.log
npm-debug.log*

# Environment
.env
.env.*
!.env.example

# Vercel
.vercel

# Vite caches
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.vite/

# TypeScript cache
*.tsbuildinfo

# NOTE: dist/ is intentionally COMMITTED — the marcdaddy.com VPS container
# serves the built dist/ folder directly from this repo. Run `npm run build`
# and commit dist/ before pushing a deployable change.
